<h1> Books Data </h1>

<table>
    <tr>
        <td>id</td>
        <td>name</td>
        <td>price</td>
    </tr>
    <% books.forEach(function(item) { %>
    <tr>
        <td><%= item.id %></td>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
    </tr>
    <% }); %>
</table>